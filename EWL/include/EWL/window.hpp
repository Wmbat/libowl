#pragma once

#if defined(VK_USE_PLATFORM_XCB_KHR)
#include <xcb/xcb.h>
#endif

#include <EGL/render_manager.hpp>

namespace EWL
{
   class window
   {
   public:
      window( );

   private:
   };
}
